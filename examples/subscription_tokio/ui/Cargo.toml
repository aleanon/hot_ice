[package]
name = "ui"
version.workspace = true
edition.workspace = true

[features]
reload = ["hot_ice", "serde"]

[dependencies]
serde = { version = "1.0.228", features = ["derive"], optional = true }
hot_ice = { path = "../../..", optional = true, features = ["tokio"] }
iced.workspace = true
