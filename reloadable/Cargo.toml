[package]
name = "reloadable"
version = "0.1.0"
edition = "2021"


[dependencies]
# deps = { path = "../deps" }
ui = { path = "../ui" }
iced.workspace = true
hot-lib-reloader = { version = "0.7.0" }
once_cell = { workspace = true }
crossfire = "1.0.1"
